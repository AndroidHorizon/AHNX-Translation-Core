# Build artifacts — not tracked on main. See the repo README for build
# instructions, or grab a prebuilt release from the launcher repo's
# Releases page. The `test` branch keeps a snapshot of built binaries for
# Aaron's own local testing convenience.
build/
*.o
*.d
*.elf
*.nacp
*.nro
*.map
*.lst
*.pfs0
*.nso
